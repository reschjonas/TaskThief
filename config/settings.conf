# TaskThief Configuration
REPORT_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../reports"
VERBOSE_MODE=true
SAFETY_LEVEL=3  # 1=Low, 2=Medium, 3=High
LOG_LEVEL="INFO"  # DEBUG, INFO, WARNING, ERROR, NONE
LOG_FILE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../logs/taskthief.log"
SUDO_AUTOASK=false